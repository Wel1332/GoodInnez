spring.application.name=goodinnez

# DB connection
# Added serverTimezone and useSSL to prevent connection errors
spring.datasource.url=jdbc:mysql://localhost:3306/dbgoodinnez?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Noelketh213
# Explicitly tell Spring to use the MySQL driver
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect